// The one translation unit that instantiates the ComputerCard
// implementation (every other file defines COMPUTERCARD_NOIMPL before
// including ComputerCard.h — see releases/82_Computer_Grids for the
// same pattern).
#include "ComputerCard.h"
